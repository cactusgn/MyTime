<Window x:Class="BindingTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BindingTest"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <StackPanel>
           
            <Button x:Name="animationBtn" Width="100" Height="100" Margin="20" Click="animationBtn_Click">Animation</Button>
            <Button Command="{Binding ShowCommand}" Width="145" Height="30" >TestShowCommand</Button>
            <TextBox Text="{Binding Name}" Margin="5" Height="30"></TextBox>
            <Slider x:Name="slider" Margin="5" ValueChanged="slider_ValueChanged"></Slider>
            <TextBox Text="" x:Name="textbox1" Margin="5" Height="30" ></TextBox>
            <TextBox Text="{Binding ElementName=slider,Path=Value}" x:Name="textbox2" Margin="5" Height="30"></TextBox>
            <TextBox Text="{Binding ElementName=slider,Path=Value}" x:Name="textbox3" Margin="5" Height="30"></TextBox>
        </StackPanel>
    </Grid>
</Window>
